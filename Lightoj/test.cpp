#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    string stMonth, edMonth;
    ll stDay, edDay, stYear, edYear;
    char temp;

    cin >> stMonth >> stDay >> temp >> stYear;
    cout << stMonth << stDay << temp << stYear;
}
