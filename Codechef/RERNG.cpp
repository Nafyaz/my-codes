#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll n;
ll p[500005];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    ll q, i;

    cin >> n >> q;

    for(i = 0; i < n; i++)
        cin >> p[i];
    
    while(q--)
    {
        cin >> l >> r;
    }
}