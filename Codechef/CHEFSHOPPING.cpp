#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define ff first
#define ss second

pair<ll, ll> call(ll st, ll ed)
{
    pair<ll, ll> ret;
    if(st+1 == ed)
    {
        if()
    }
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    ll T, n, i;

    cin >> T;

    while(T--)
    {
        cin >> n;

        for(i = 1; i <= n; i++)
            cin >> L[i];
        for(i = 1; i <= n; i++)
            cin >> R[i];

        cout << call(1, n).ff << "\n";
    }
}
