#include<bits/stdc++.h>
using namespace std;

int main()
{
    int x, n;
    set<int> pos;
    
    cin >> x >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> p[i];

        pos.insert(p[i]);
    }

    vector<int> ans;

    for (int ; i)
}