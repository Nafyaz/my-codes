#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll  d[1000005];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    ll T, caseno = 0, n, i, d;

    cin >> T;

    while (T--)
    {
        cin >> n;

        for(i = 0; i < n; i++)
            cin >> d[i];

        cout << 
    }
}